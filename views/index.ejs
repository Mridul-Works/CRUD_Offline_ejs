<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 ">
    <h1 class="text-[3rem] text-center text-white "> Make notes </h1>

    <div class="form p-10">
        <form action="/create" method="post">
            <input class="block mt-10 px-5 py-2 bg-zinc-800 w-full rounded-md"  type="text" placeholder="Title" name="title">
            <textarea  class="block mt-10 px-5 py-2 bg-zinc-800 resize-none w-full rounded-md" placeholder="Write your task details here" name="details" ></textarea>
            <input class="mt-10 bg-indigo-600 text-white px-8 py-1 rounded-md" type="submit" value="Create task">
        </form>
    </div>
    <div class="tasks p-10 flex gap-3 flex-wrap">

        <% if(files.length >0){%>
        <% files.forEach(function(val){ %>
             <div class="task w-72  px-3 py-4 bg-zinc-800 rounded-md">
            <h1 class="text-white  text-3xl tracking-tight"><%= val %>  </h1>
            <div class="flex w-full justify-between items-center mt-10">

                 <a class="text-blue-500 inline-block " href="/files/<%= val  %>">Read more...</a>
            <a class="text-zinc-600" href="/edit/<%= val %>" <%= val %>>edit filename</a>
            </div>
           
        </div>

      <%  }) %>
        <% } else{ %>
            <h3 class="text-zinc-400">No tasks yet</h3>
        <% }%>

    </div>

    </div>
</body>
</html>